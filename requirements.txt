# =============================================================================
# OFA AI Assist — Production Dependencies
# Pin major versions; use >= for patch flexibility.
# =============================================================================

# --- Web Framework -------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0                   # Async HTTP client (used internally by FastAPI test client)

# --- LangChain (latest stable) -------------------------------------------
langchain>=0.2.0
langchain-openai>=0.1.0         # Azure OpenAI chat + embedding wrappers
langchain-community>=0.2.0      # Document loaders (SharePoint, PDF, DOCX)
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0

# --- Azure ------------------------------------------------------------------
azure-identity>=1.17.0          # DefaultAzureCredential, ClientSecretCredential
azure-search-documents>=11.6.0  # Azure AI Search SDK
azure-storage-blob>=12.20.0     # Optional: blob storage for processed docs
openai>=1.30.0                  # Underlying OpenAI SDK (used by langchain-openai)

# --- SharePoint -------------------------------------------------------------
Office365-REST-Python-Client>=2.5.12   # SharePoint REST API client

# --- Document Parsing -------------------------------------------------------
pypdf>=4.2.0                    # PDF parsing
python-docx>=1.1.0              # DOCX parsing
beautifulsoup4>=4.12.0          # HTML parsing
lxml>=5.2.0                     # HTML/XML parser backend

# --- Configuration & Validation ---------------------------------------------
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# --- Streamlit UI -----------------------------------------------------------
streamlit>=1.35.0
streamlit-chat>=0.1.1           # Chat message component

# --- Utilities --------------------------------------------------------------
tenacity>=8.3.0                 # Retry logic
tiktoken>=0.7.0                 # Token counting
structlog>=24.1.0               # Structured logging
python-multipart>=0.0.9         # FastAPI file upload support

# --- Testing ----------------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
respx>=0.21.0                   # Mock httpx
